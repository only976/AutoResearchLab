"""
Shared layout constants for Decomposition Tree and Execution Graph.
Unified spacing ensures consistent connection line lengths across both views.
"""

# Node dimensions (compact: only merged nodes show count, others are mostly empty)
DEFAULT_NODE_W = 30
DEFAULT_NODE_H = 22

# Horizontal spacing between sibling nodes
DEFAULT_NODE_SEP = 16

# Vertical spacing between layers (rank separation)
# Same value for both views so connection lines have consistent length
DEFAULT_RANK_SEP = 30

# Canvas padding
DEFAULT_PADDING = 24
